name: V-Slice Mobile (Global Libs Build)
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          submodules: 'recursive' # Keeps the PC asset structure

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Haxe 4.3.7 (Manual Stable Download)
        run: |
          wget https://github.com/HaxeFoundation/haxe/releases/download/4.3.7/haxe-4.3.7-linux64.tar.gz
          mkdir -p ~/haxe && tar -xf haxe-4.3.7-linux64.tar.gz -C ~/haxe
          echo "$HOME/haxe/haxe-4.3.7" >> $GITHUB_PATH
          export HAXEPATH=$HOME/haxe/haxe-4.3.7

      - name: Install Global Libraries (The PC Way)
        run: |
          haxelib setup ~/haxelib
          # Core Framework
          haxelib install lime 8.1.2
          haxelib install openfl 9.3.3
          haxelib install flixel 5.5.0
          haxelib run lime setup flixel
          haxelib run lime setup
          
          # Essential PC/V-Slice Global Libs
          haxelib install flixel-addons
          haxelib install flixel-ui
          haxelib install hscript-iris
          haxelib git funkin.vis https://github.com/FunkinCrew/funkVis.git
          
          # Verified Mobile Sync & Stability Libs
          haxelib git extension-webm https://github.com/GrowtopiaFli/extension-webm
          haxelib git hxvlc https://github.com/MAJigsaw77/hxvlc.git
          haxelib git extension-androidtools https://github.com/MAJigsaw77/extension-androidtools.git
          
          haxelib upgrade -y

      - name: Setup Android NDK (r25c)
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r25c

      - name: Configure Lime
        run: |
          haxelib run lime config ANDROID_SDK $ANDROID_HOME
          haxelib run lime config ANDROID_NDK_ROOT $ANDROID_NDK_HOME
          haxelib run lime config JAVA_HOME $JAVA_HOME
          haxelib run lime config ANDROID_SETUP true

      - name: Global Rebuild & Compile
        run: |
          haxelib run lime rebuild extension-webm android
          haxelib run lime rebuild hxvlc android
          haxelib run lime build android -final \
          -DAPP_TITLE="Friday Night Funkin': Vslice mobile" \
          -DAPP_PACKAGE="com.Funkin.Mobile"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: VSlice-Mobile-Global
          path: export/release/android/bin/app/build/outputs/apk/release/*.apk
