name: Sync with FNF Upstream

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout your repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add FNF upstream remote
        run: git remote add upstream https://github.com/FunkinCrew/Funkin.git

      - name: Fetch upstream
        run: git fetch upstream

      - name: Force overwrite with upstream files
        run: |
          git checkout main
          git reset --hard upstream/main

          # Remove upstream's workflow files — we don't want them
          rm -rf .github/workflows/

          # Restore YOUR workflow files from before the reset
          git checkout HEAD@{1} -- .github/workflows/

      - name: Update submodules
        run: git submodule update --init --recursive

      - name: Push to main
        run: git push origin main --force
